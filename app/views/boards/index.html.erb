<div class="container">

  <hr class="featurette-divider">

  <div class="scroll">
    <div class="container">
      <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-5">

        <!-- お題写真 -->
        <% @board.each do |b| %>
          <div class="col">
            <div class="card shadow-sm">
                <%= link_to image_tag(b.image.url, class: 'border border-warning border-5 shadow bg-white rounded card-img-top', size: '400x300'), show_path %>
            </div>
          </div>
        <% end %>

      </div>
    </div>
  </div>

  <hr class="featurette-divider">

</div>

<%= render 'shared/footer' %>
